// src/game/utils.js

// Example utility function (customize as needed)
export const isValidMove = (move) => {
    // Add logic to validate move if necessary
    return move && move.sourceSquare && move.targetSquare;
  };
  